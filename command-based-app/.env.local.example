# Local Environment Configuration for Ollama + GPT-OSS
# Copy this file to .env to use these settings
#
# Example:
#   cp .env.local.example .env

# =============================================================================
# LLM Configuration - Ollama + GPT-OSS
# =============================================================================

# LLM Service: Use "Ollama" for local models, "OpenAI" for cloud, "Gemini" for Google Gemini, "Groq" for Groq API
LLM_SERVICE=Ollama

# Ollama Configuration
OLLAMA_BASE_URL=http://localhost:11434/v1
OLLAMA_MODEL=gpt-oss

# GPT-OSS Reasoning Level
# Options: low, medium, high
# - low: Faster, less detailed reasoning
# - medium: Balanced performance and quality (recommended)
# - high: More thorough reasoning, slower
GPT_OSS_REASONING=medium

# =============================================================================
# Database Configuration
# =============================================================================
DATABASE_PATH=email_pipeline_local.db

# =============================================================================
# Logging Configuration
# =============================================================================
LOG_LEVEL=INFO  # Options: DEBUG, INFO, WARNING, ERROR

# =============================================================================
# Gmail API Configuration (if not using credentials.json)
# =============================================================================
# GOOGLE_CLIENT_ID=your_client_id_here
# GOOGLE_CLIENT_SECRET=your_client_secret_here

# =============================================================================
# Optional: OpenAI Fallback (if you want to use OpenAI sometimes)
# =============================================================================
# Uncomment these lines if you want to switch between Ollama and OpenAI
# OPENAI_API_KEY=your_openai_api_key_here
# OPENAI_MODEL=gpt-4o-mini

# =============================================================================
# Optional: Gemini API (OpenAI-compatible endpoint)
# =============================================================================
# LLM_SERVICE=Gemini
# GEMINI_API_KEY=your_gemini_api_key_here
# GEMINI_MODEL=gemini-2.5-flash
# GEMINI_BASE_URL=https://generativelanguage.googleapis.com/v1beta/openai/

# =============================================================================
# Optional: Groq API
# =============================================================================
# LLM_SERVICE=Groq
# GROQ_API_KEY=your_groq_api_key_here
# GROQ_MODEL=llama-3.1-8b-instant

# =============================================================================
# Performance Tuning
# =============================================================================
# MAX_CONTENT_LENGTH=4000  # Max characters per email
# DEFAULT_BATCH_SIZE=50    # Number of emails to process per batch

# =============================================================================
# File Paths Configuration (optional)
# =============================================================================
# By default, files are stored in platform-appropriate locations:
#   - Linux/Mac: ~/.local/share/gmail-llm-labeler/
#   - Windows: %LOCALAPPDATA%/gmail-llm-labeler/
#
# You can override these paths with environment variables OR by using a YAML
# config file (specify with CONFIG_FILE). Environment variables take precedence.
#
# CONFIG_FILE=/path/to/your/config.yaml
# DATABASE_FILE=/custom/path/to/email_pipeline.db
# LLM_LOG_FILE=/custom/path/to/logs/llm_interactions.jsonl
# ERROR_LOG_FILE=/custom/path/to/logs/categorization_errors.log
# TEST_OUTPUT_FILE=/custom/path/to/test_results.csv
# TEST_SUMMARY_FILE=/custom/path/to/test_summary.json